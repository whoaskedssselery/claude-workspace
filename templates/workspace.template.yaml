version: 1
toolVersion: {{VERSION}}

preset: {{PRESET}}

core:
{{CORE_LIST}}

skills:
{{SKILLS_LIST}}

external:
{{EXTERNAL_LIST}}

# Skills pulled in from an arbitrary repo via "claude-workspace add <url>"
# (through `npx skills`), as "name=source" — not part of this package's own
# catalog, so `sync` re-fetches each one from its recorded source instead.
remote:
{{REMOTE_LIST}}
